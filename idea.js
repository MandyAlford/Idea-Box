class Idea {
  constructor(id, title, body) {
    this.id = id;
    this.title = title;
    this.body = body;
    this.favorite = false;
  }

  saveToStorage(id) {
  }

  deleteFromStorage(id) {
  }

  updateIdea(id) {
  }
}
